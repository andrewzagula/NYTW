// Test stub for the `server-only` package, which is a build-time guard with no
// runtime behavior. Aliased in vitest.config.ts so server modules can be unit
// tested without pulling in the real package.
export {};
